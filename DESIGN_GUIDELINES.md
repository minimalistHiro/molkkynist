# DESIGN_GUIDELINES

## デザイン方針

Molkkynist のデザインは、Google Stitch で作成した `Fjord & Field` を主なデザイン方針として採用します。

北欧由来のミニマルさ、芝生の上で遊ぶ開放感、木製モルック道具の手触り、初心者が参加しやすいコミュニティ感を組み合わせた、`Modern-Organic` な公式サイトを目指します。

競技スポーツらしい強さや大会感よりも、晴れた日の公園で自然に人が集まるような、親しみやすく信頼できる印象を優先します。

今後のデザイン調整では、既存の `Modern-Organic` 方針を保ちつつ、芝生の爽やかさ、モルック道具の木の質感、キャラクターによる案内役の親しみやすさを少し強めます。過剰な装飾ではなく、重要な導線や見出し周辺に絞って「木」「芝生」「キャラクター」を感じられる要素を配置します。

## 表現したい印象

- コミュニティ感
- 初心者歓迎
- 自然体で親しみやすい
- 北欧ミニマル
- 芝生や屋外活動の爽やかさ
- 木の道具を使うスポーツらしい温かみ
- キャラクターが案内してくれるようなポップさ
- 公園や広場で過ごすような開放感
- 協力団体やスポンサーにも見せられる信頼感

## 避けたい印象

- 競技大会のように強すぎるスポーツ感
- 高級ブランドのような硬さ
- 企業サイトのような冷たい印象
- 派手なグラデーションや過剰な装飾
- 緑だけで単調な画面
- 内輪感が強すぎるコミュニティ表現

## カラーパレット方針

CSS ではカスタムプロパティで色を管理します。

Stitch の `Fjord & Field` で定義された色を主採用し、芝生、木製ピン、北欧のやわらかい光を感じる配色にします。

### 基本色

| 用途 | 色名 | 値 | 使い方 |
| --- | --- | --- | --- |
| 背景 | Ivory Surface | `#fafaf5` | ページ全体の基本背景 |
| 明るい面 | White Surface | `#ffffff` | カードや浮いた面 |
| 薄い面 | Surface Container | `#eeeee9` | セクション背景、控えめな区切り |
| 本文 | On Surface | `#1a1c19` | 見出し、本文 |
| 補助本文 | On Surface Variant | `#3f4a3c` | 説明文、メタ情報 |
| 境界線 | Outline Variant | `#becab9` | 区切り線、カード枠 |

### ブランド色

| 用途 | 色名 | 値 | 使い方 |
| --- | --- | --- | --- |
| メイン | Grass Green | `#006e1c` | 主要ボタン、重要な導線、ブランドアクセント |
| 明るい緑 | Fresh Green | `#4bae4f` | ヒーローやカード内のアクセント |
| ライム | Lime Accent | `#dded49` | チップ、状態表示、軽い強調 |
| 木の色 | Wood Brown | `#75584d` | アイコン、細い枠線、影、木の道具らしさ |
| 木の明るい色 | Soft Wood | `#b59387` | 背景アクセント、柔らかい装飾 |

### セマンティックカラー

- 成功や受付中の状態は `Grass Green` を基準にする。
- 注意や補足は、自然なオーカー系の色味を使う。
- エラーは強すぎる赤ではなく、落ち着いたテラコッタ系を使う。

### 色使用時の注意点

- 背景は純白ではなく、基本的に `#fafaf5` のアイボリーを使う。
- 緑はCTAやアクセントに集中させ、画面全体を緑一色にしない。
- 木目や芝生素材を使う場合も、本文の可読性を優先する。
- 背景画像の上に長文を直接置かない。
- コントラスト不足を避け、ボタンやリンクは視認性を確保する。
- 木目CTAやキャラクターを使う画面では、背景の罫線・柄は控えめにし、一色のアイボリーや淡い芝生色の面で整理する。
- セクション背景は、基本のアイボリー（`--color-surface-warm`）と淡い芝生色（`--color-surface-field`）の一色面を使い分ける。薄いグレー面は必要以上に増やさず、罫線より余白と背景面で区切る。

## フォント方針

Molkkynist は日本語サイトのため、日本語本文の読みやすさを最優先します。

Stitch では見出しに `Plus Jakarta Sans`、本文に `Be Vietnam Pro` が提案されていますが、日本語表示では以下の方針で扱います。

### 基本フォント

- 日本語本文: `Noto Sans JP`、`Hiragino Sans`、`Yu Gothic`、system sans-serif を優先する。
- 英字ロゴ周辺や短い英字見出し: `Plus Jakarta Sans` を補助的に使う。
- UIテキストや本文: 日本語の可読性を優先し、欧文フォントだけに依存しない。

### 文字階層

| 用途 | サイズ目安 | ウェイト | 行間 |
| --- | --- | --- | --- |
| ヒーロー見出し | 30px - 40px | 700 | 1.2 |
| セクション見出し | 24px - 32px | 600 | 1.25 |
| カード見出し | 20px - 24px | 600 | 1.35 |
| 本文 | 16px - 18px | 500 | 1.6 - 1.75 |
| 補足・ラベル | 12px - 14px | 500 - 600 | 1.4 |

### タイポグラフィの注意点

- スケールを大きくしすぎず、ウェイト差で階層をつくる。
- 会話的で親しみやすい印象を保つため、英字のオールキャップは小さなラベルに限定する。
- スマートフォンでも文字が詰まりすぎないよう、本文の行間を十分に取る。
- 文字間を極端に詰めない。
- 参考方向として Mölkky Mania のような太めの日本語ゴシック感を取り入れる。ただし本文まで全て 800 以上にせず、見出し・CTA・チップを 800 - 900、本文は 500 程度を基本にして読みやすさを残す。
- セクション見出し、ページ冒頭見出し、左右分割レイアウト先頭の見出しは太めの日本語ゴシックを基準にし、短い芝生色の下線など小さなアクセントで階層を示す。装飾は見出し周辺に限定し、本文エリアには持ち込まない。

## レイアウト方針

Stitch の Fluid Grid を採用し、静的HTML/CSSで扱いやすいレスポンシブレイアウトにします。

### グリッド

- デスクトップ: 12カラム、24pxガター、左右48px余白、最大幅1200px。
- タブレット: 8カラム、20pxガター、左右32px余白。
- モバイル: 4カラム、16pxガター、左右16px余白。

### 余白

- 8pxを基本単位にする。
- セクション間は広めに取り、線や装飾ではなく余白で情報を分ける。
- 主要セクションでは 48px 以上の縦余白を基準にする。
- カード内は 16px - 24px の余白を基本にする。

### ページ構成

- セマンティックHTMLを優先する。
- 見出し階層を崩さない。
- モバイルファーストで設計する。
- PC表示では余白を活かし、情報を整理して見せる。
- トップページは第一印象を強くしつつ、次のセクションが見える構成にする。
- トップページのヒーローは、活動写真を背景に敷き、白いぼかしオーバーレイの上にはタイトルのみを配置する。スマートフォンで写真が隠れすぎないよう、説明文やCTAは画像上に重ねない。ヒーロー直下にコンセプト見出し、長めの紹介文、開催スケジュールへ進む主要CTAを置き、活動写真とコミュニティ説明を分けて見せる。
- カードはイベント、活動レポート、メンバーなどの繰り返し要素に使用する。
- メンバーカード（`.member-card`）は、コンパクトな円形プレースホルダー（直径96px、`.person-placeholder` を `border-radius: 50%` で円形化）と「役割（`.card-kicker`）＋名前」のみで構成する。本文や引用は詳細ページに集約する。テキストは中央寄せ。リンク化されており、ホバー・フォーカスで影が強まりカードが2px浮く。グリッド列数は PC=3 / タブレット=2 / スマートフォン=2 で統一。詳細ページ側（`.member-detail__visual`）は直径200pxの大きな円形プレースホルダーで同じシルエットを表示する。
- メンバー詳細ページ（`member.html`）の構成は、ページヒーローを `Members` ラベルのみに簡素化し（`.member-detail .page-hero { border-bottom: 0; }` で他ページの page-hero の下線を継承させない）、本文セクションの先頭にアイコン（`.member-detail__visual`）→ 役割（`.member-detail__role`）→ 名前（h1）の順で配置し、`.member-detail__heading` で役割と名前を中央寄せでまとめる。本文（`.prose`）はその下に通常配置。
- トップページの「活動の様子」セクション（`#activity`）は、既存9枚の写真をランダム感のある順序で並べたコラージュ型 `activity-grid` で見せる。PCでは6列グリッドを基準に、large / wide / tall / medium / small のサイズ差を付ける。タブレットでは4列、スマートフォンでは2列に落とし、最初の大きな写真と横長写真は2列幅で表示する。写真クリックは不可（装飾のみ）。画像は `assets/images/activity/activity-01.jpg` 〜 `activity-09.jpg` を参照する。セクション上部には `reports.html` への導線ボタン「活動レポートを見る」を残す。
- トップページの「お知らせ」セクションは、`report-carousel` / `report-news-card` のカルーセル UI で、正方形ビジュアル＋日付＋タイトルで構成したニュースカードを横スライドする。PC は3枚グリッド、タブレットは2枚グリッドで表示し、スマートフォンは中央1枚＋両隣のカードが見切れるピーク表示にする。スマートフォンでは非アクティブ（両隣）カードは半透明で表示し、タップするとそのカードに送って中央に強調する。加えて横スワイプでも前後カードへ切り替えられる。5秒間隔で自動送りし、`prefers-reduced-motion` 有効時、ホバー・フォーカス時、タブ非表示時は自動送りを停止する。前後ボタンやドットインジケータは設けない。カード遷移先は `news.html?id=xxx`。
- トップページの重要CTAには、左右のキャラクターが案内しているような「案内役CTA」を限定採用する。中央の文言は画像内文字ではなくHTMLテキストで配置する。トップページ「参加の流れ」の案内CTAに限り、木目板画像をCTA本体に使う。

## 角丸・影・立体感

木製モルック道具の丸みや、芝生の上に置いた道具のやわらかい影を意識します。

### 角丸

- 小さな要素: 4px。
- 通常カード、入力風要素: 8px。
- 大きなビジュアルカード、プロフィールカード: 16px。
- ボタンはサイト全体でカプセル型を基本にする。
- 24px以上の角丸は、ボタンやヒーロー内の大きな装飾、特別なビジュアルに限定する。

### 影

- 黒い強い影は使わない。
- 影には Wood Brown を低透明度で使い、温かい印象にする。
- 目安は `rgba(117, 88, 77, 0.04)` から `rgba(117, 88, 77, 0.08)`。
- カード、主要CTA、アクティブ状態など、触れる要素に限定して使う。

## UI方針

### ボタン

- 主要ボタンはカプセル型、`Grass Green` の単色背景、白文字を基本にする。
- 主要ボタンはグラデーションを使わず、必要に応じて控えめな影で押せる要素として見せる。
- 副ボタンはカプセル型、白背景、Wood Brown 系の細い枠線を基本にする。
- Instagram DM への導線を最重要CTAとして扱う。
- 参加申し込みやInstagram DMなど、ページ内で最も重要な通常ボタンも、カプセル型・単色背景を基本にする。
- フッター直前の参加CTAは、緑背景に白文字の反転UIとして扱う。見出しは大きめに置き、Instagram DMボタンは白背景＋Grass Green文字にして、緑の面の中で最も押せる要素として見せる。Instagram URLが未確定の間は仮リンク `#` で置く。
- キャラクター付きCTAを使う場合も、すべてのリンクを装飾CTAに置き換えない。通常ボタンは一覧、カード、フォーム、フッター固定導線などで継続利用する。
- トップページ「参加の流れ」の案内役CTAでは、左右キャラクターを個別の背景透明PNG（`assets/images/characters/molkkynist-character-green.png` / `molkkynist-character-brown.png`）として扱い、CTA本体には木目板画像（`assets/images/ui/wooden-cta-board.png`）を使う。木板画像には文字を含めず、中央テキストはHTMLで重ねる。

### チップ・状態ラベル

チップは、イベントやレポートの状態を軽く示すために使います。

使用例:

- `受付中`
- `開催予定`
- `終了しました`
- `初心者歓迎`
- `見学歓迎`

チップはピル型にし、淡いグリーンやライムを背景に使います。

### カード

- カード背景は白を基本にしつつ、背景面になじむよう `rgba(255, 255, 255, 0.78 - 0.86)` 程度の軽い白面を使う。
- 枠線は Wood Brown または Outline Variant を低透明度（0.12程度）で使う。
- 影は `--shadow-card` を基準に控えめにし、ホバー・フォーカス時のみ `--shadow-card-hover` で少し強める。
- カード内には日付、場所、状態、短い説明、CTAを整理して配置する。
- SNSリンクのロゴ画像は視認性を優先し、カードUI軽量化の対象には含めない。

### リスト

- リストは薄いアイボリーグレーの線で区切る。
- アイコンを使う場合は、丸みのあるものを選び、Wood Brown や Grass Green を使う。
- 情報を詰め込みすぎず、日付や場所など判断に必要な情報を先に見せる。
- トップページFAQはコンパクトなアコーディオンリストにする。質問行は白背景の横長ボックス、回答は初期非表示、開閉アイコンは背景なしの黒いくの字型のみで控えめに表示する。

### フォーム

公開サイトのお問い合わせフォーム（D-1）と、管理画面の入力 UI で使用します。

- 入力欄は `White Surface` 背景、`Wood Brown` 系の細い枠線（透明度0.28程度）を基本にする。
- フォーカス時のアウトラインは Lime Accent（`rgba(221, 237, 73, 0.95)`）を3px外側に表示し、視認性を確保する。
- 入力欄、ラジオ、チェックボックス、トグルは角丸 `var(--radius-md)`（8px）を基準にする。
- 必須／任意ラベルはピル型バッジとし、必須=`Grass Green` 系、任意=`Wood Brown` 系の淡背景に揃える。
- ラジオ／チェック／日程トグルは、選択時に Lime Accent の淡背景＋Grass Green 枠で「選ばれている」ことを示す。
- 送信ボタンは主要ボタンと同じ `Grass Green` 系。送信中・成功・エラーのステータス表示はそれぞれライム／グラスグリーン／オーカーの淡背景で控えめに伝える。
- フォームカード全体は `var(--radius-lg)`（16px）と `--shadow-soft` を使い、ページ内で「入力エリア」として浮かせる。
- プライバシーポリシー同意のチェックは、フォーム末尾に独立した行として配置し、リンクは Grass Green の下線で示す。

### 管理画面

管理画面は公開サイトと同じ `Fjord & Field` の色・角丸・フォームUIを使いながら、更新作業がしやすい密度に寄せます。

- `/admin/` 配下の画面は、公開サイトのヘッダー・フッターとは分けた簡潔な管理ヘッダーを使う。
- 編集フォームと一覧は2カラムを基本にし、モバイルでは1カラムへ落とす。
- 管理メニュー、イベント一覧、お問い合わせ一覧は白背景のカードでまとめ、状態は `status-chip` で表示する。
- 入力欄、セレクト、日付・時間・数値入力は公開フォームと同じ枠線・フォーカス表現を使う。
- ログイン画面は中央配置の単一カードにし、未設定時は送信ボタンを無効化して設定待ちの状態を明示する。

### 採用しない・限定採用の要素

- スコアボードは公開サイトの主要コンポーネントにはしない。将来、モルック説明やイベントレポート内で必要になった場合のみ参考にする。
- 管理画面へのリンクは一般ページには原則表示しない。

## 画像素材方針

画像素材は ChatGPT の Image Gen 2 を使用して作成する方針です。

### 画像最適化ルール

サイトに画像を追加・差し替えするときは、公開HTMLがカメラ原寸や未圧縮に近い大容量画像を直接読み込まないよう、必ずWeb表示用に軽量化してから `assets/` に配置します。

- 写真は表示枠の最大幅を基準に、通常版は最大 1200px 程度、スマートフォン向け派生版は 800px 程度を目安に作成する。
- 正方形グリッドやカード用サムネイルは、通常版 800px 四方、スマートフォン向け派生版 400px 四方を基本にする。
- JPEG 写真は品質 70〜75 を目安に書き出し、画質よりも表示速度を優先する。特別に大きく見せる写真でも 500KB 未満を目安にする。
- ヘッダー、フッター、ボタン付近で表示するロゴやアイコンは、実表示サイズの2〜3倍程度の軽量版を使用する。巨大なOGP用画像をそのままUI表示に使わない。
- 同じ画像を複数サイズで用意する場合は、HTML 側で `srcset` と `sizes` を指定し、画面幅に応じて小さい画像が選ばれるようにする。
- 画像要素には `width` / `height` を入れてレイアウトシフトを防ぐ。ファーストビュー外の画像には `loading="lazy"` と `decoding="async"` を付ける。
- 原画が必要な場合は公開HTMLから参照しない場所で別途管理し、公開用 `assets/` には最適化済みファイルのみを置く。

### ロゴ素材

Molkkynist のロゴ素材は以下を使用します。

- 通常ロゴ: `assets/images/logos/molkkynist-logo.png`
- UI表示用ロゴマーク: `assets/images/logos/molkkynist-logo-mark.png`
- タイトルロゴ: `assets/images/logos/molkkynist-title-logo.png`
- ヘッダー用ワードマーク: `assets/images/logos/molkkynist-wordmark.png`
- ヒーロー用ワードマーク: `assets/images/logos/molkkynist-wordmark-hero.png`

通常ロゴは OGP / Twitter Card / JSON-LD など、シェアや構造化データ上のブランド識別に使用します。
UI表示用ロゴマークはヘッダー、フッター、SNS誘導付近など、ページ上で小さく表示するブランド識別に使用します。
タイトルロゴ（イラスト版）は Molkkynist 名を強く見せたい告知物などで使用します。
ヘッダー用ワードマークは、ヘッダーの通常ロゴ横に配置するブランド名画像として使用します。ヘッダー高さやナビゲーションと干渉しないサイズに制御します。
ヒーロー用ワードマークは、ヘッダー用ワードマークの下端ディセンダー（`y`の脚）が大きく表示した際に見切れないよう、上下に透明余白を追加した派生版です（1008x183）。現在のトップページヒーローでは写真上の情報量を絞るため使用せず、必要になった場合の告知物・派生デザイン用アセットとして保管します。

### ヒーロー画像

トップページのヒーローは、実際の活動写真を背景に敷き、写真上にはタイトルだけを重ねます。説明文と主要CTAは写真下に分離し、スマートフォンでも写真の雰囲気が見える構成を優先します。

- ヒーロー写真: `assets/images/hero/hero-main.jpg`
- スマートフォン向け派生版: `assets/images/hero/hero-main-800.jpg`
- 角丸＋ソフトシャドウで配置し、写真上のタイトルは小さめに抑えて余白を確保する。
- HTML では `srcset` / `sizes` を使い、表示幅に応じて 800px 版と 1200px 版を読み分ける。
- 写真は実際の活動の様子（生成画像ではなく実写）を使用する。差し替え時も同様に屋外の明るい雰囲気を優先する。

### お知らせ・レポート系ビジュアル枠

お知らせカード、レポートカード、お知らせ詳細ページ `news.html` 上部に表示する記事ビジュアルです。Firestore `news` 連携前のダミー期間は、画像を持たないため、淡い単色のカラープレースホルダーで代用します。

- セレクタ: `.report-news-card__visual`、`.report-card__visual`、`.news-detail__visual`
- トップのお知らせカードは正方形、詳細ページは `16 / 9`・最大幅 800px を基準にする。
- 角丸はトップのお知らせカードとカード内ビジュアルで `var(--radius-news-card)`、レポートカード内ビジュアルで `var(--radius-md)`、詳細ページの大きなビジュアルで `var(--radius-lg)` を使う。影は控えめな `--shadow-card` を基準にする。
- カラー: `--color-surface-field` を基本に、`--soft` / `--wood` / `--lime` のモディファイアで淡い芝生色、木色、ライム色を使い分ける。
- 強いグラデーションは使わず、薄い枠線や控えめな内側線で面を整理する。実写写真や生成画像が入ったときに、プレースホルダーが主張しすぎない状態を優先する。
- 将来 Firestore に画像URLを持たせた段階で、`img` 要素または `background-image` へ差し替える前提。

### About セクション画像

トップページ `#about`（モルックとは）の本文には、活動の雰囲気が伝わる実写を挟み込み、文章だけで終わらない構成にします。

- 配置場所: `assets/images/about/`
- 現状の素材: `about-photo-01.jpg`（1段落目の直後）、`about-photo-02-img-8059.jpg`（2段落目の直後）
- スマートフォン向け派生版: `about-photo-01-800.jpg`、`about-photo-02-img-8059-800.jpg`
- スタイル: `.prose__image`（`width: 100% / 角丸 / ソフトシャドウ / object-fit: cover`）。`.prose` の `gap` で段落・リンクと自然に区切る。
- 画像はWeb表示用に長辺 1200px 程度へリサイズし、JPEG 品質 70〜75 を目安に最適化してから配置する（原画は別途保管）。
- HTML では `srcset` / `sizes` を使い、表示幅に応じて 800px 版と 1200px 版を読み分ける。
- 撮影写真を優先。差し替えや追加時も屋外の明るい雰囲気を維持し、参加者の顔出し可否を確認のうえ使用する。

### 活動の様子セクション画像

トップページ `#activity`（活動の様子）の 3×3 グリッドに表示する活動写真です。

- 配置場所: `assets/images/activity/`
- ファイル名: `activity-01.jpg` 〜 `activity-09.jpg`（左上から右下へ、`activity-01` `activity-02` `activity-03` / `activity-04` `activity-05` `activity-06` / `activity-07` `activity-08` `activity-09` の順）
- 想定アスペクト比: 正方形（CSS で `aspect-ratio: 1 / 1` / `object-fit: cover`）。原画が長方形でも CSS 側でトリミングされる
- 画像はWeb表示用に 800px 四方へリサイズし、JPEG 品質 70〜75 を目安に最適化してから配置する
- スマートフォン向けに `activity-01-400.jpg` 〜 `activity-09-400.jpg` の 400px 四方の軽量版も用意し、`srcset` で表示幅に応じて読み分ける
- 撮影写真を優先。屋外の活気が伝わるカットを選び、顔出し可否を確認のうえ使用する
- 画像未配置の枠は `.activity-grid__item` の淡い芝生色の面がプレースホルダーとして表示される。写真が主役になるよう、背景の装飾は強めない

### 作成候補

- 芝生のテクスチャ
- 木目の背景素材
- モルックのスキットルをイメージしたビジュアル
- 屋外でモルックを楽しむコミュニティのイメージ
- メンバー紹介用の柔らかい背景
- イベント告知用の汎用ビジュアル
- OGP / Twitter Card 用のシェア画像（推奨サイズ 1200x630px、Molkkynist ロゴ＋キャッチコピーを構成。確定までは暫定で `assets/images/logos/molkkynist-logo.png` を流用）

配置場所:

```text
assets/
```

### 画像使用時の注意点

- 生成画像は、実在の人物写真と誤認されない使い方にする。
- 実際のイベント写真がある場合は、生成画像と役割を分ける。
- 背景画像は軽量化して使用する。
- 重要な文字情報を画像内だけに入れない。
- 代替テキストを設定する。

## レスポンシブ方針

### モバイル

- 1カラムを基本にする。
- ボタンは押しやすいサイズにする。
- イベント情報は日付、場所、参加導線を優先して表示する。
- ナビゲーションはハンバーガーメニュー化し、右上の三本線ボタンから主要セクションを縦並びで開閉できるようにする。
- 長い見出しやボタン文言が折り返しても破綻しないようにする。

### タブレット

- 2カラム構成を部分的に使う。
- カードグリッドは2列を基本にする。
- 余白を増やし、モバイルより情報のまとまりを見せやすくする。

### デスクトップ

- 最大幅1200pxを基準に中央配置する。
- イベントやレポートはカードグリッドで表示する。
- ヒーローエリアではビジュアルとコピーのバランスを取る。
- セクション間の余白を使い、読み進めやすい流れをつくる。

## 実装時のCSS管理

CSS は `css/styles.css` を基準に管理します。

カラートークン例:

```css
:root {
  --color-surface: #fafaf5;
  --color-surface-container: #eeeee9;
  --color-surface-card: #ffffff;
  --color-text: #1a1c19;
  --color-text-muted: #3f4a3c;
  --color-outline: #becab9;
  --color-primary: #006e1c;
  --color-primary-soft: #4bae4f;
  --color-secondary: #dded49;
  --color-wood: #75584d;
  --color-wood-soft: #b59387;
  --shadow-soft: 0 4px 12px rgba(117, 88, 77, 0.08);
  --radius-sm: 4px;
  --radius-md: 8px;
  --radius-news-card: 12px;
  --radius-lg: 16px;
  --container-max: 1200px;
}
```

### パートナー枠（協力団体・スポンサー）

トップページ末尾の「協力団体・スポンサー」セクションに使うレイアウトです（参考: モルックマニアの協賛パートナー紹介）。

- 掲載済みロゴのみを横に並べ、ロゴ数に応じて `auto-fit` で自然に折り返す。
- 1枠 = 1ロゴのみ。団体名や役割テキストは枠内に併記せず、必要な紹介文はセクション冒頭のリード文と下部CTAで補う。
- ロゴ枠にはカード背景や影を付けず、透過ロゴそのものが自然に見える余白で配置する。ロゴ画像は `max-width: 180px / max-height: 104px / object-fit: contain` でアスペクト比を保って中央配置する。
- ロゴ画像は `assets/images/partners/` に PNG（背景透過を優先、必要に応じて白背景）で配置し、ファイル名は半角英数のスラッグ（例: `cocoshiba-transparent.png`）にする。`alt` には正式名称（例: `Antenna Books & Cafe ココシバ`）を入れる。
- ロゴ未掲載の枠は表示しない。募集案内はセクション下部のCTAで補う。
- セクション下部に「協力・スポンサーシップのご相談」CTA を配置し、お問い合わせフォームと Instagram DM の双方へ導線を出す。
- 旧「ロゴ＋団体名＋役割の3段カード」レイアウトは廃止済み（2026-05-23 反映）。

### 月次スケジュールカレンダー

トップページ「開催スケジュール」で使う月次カレンダーのレイアウトです。

- 上部に月見出しと前月／翌月ナビゲーション（円形ボタン）。
- 7列グリッドで日／月／火／水／木／金／土を表示し、日曜は赤系、土曜は青系、祝日扱いは未対応。
- 当日は緑のアウトラインで強調、Firestore `events` で公開されたイベント日はセル背景を淡い緑にし、下部にドットマーカーを表示する。
- カレンダー下に当月分のイベント一覧（日付＋タイトル＋時間／場所）を縦に並べる。該当なしの場合は破線枠で「この月の開催予定はまだ登録されていません。」と表示する。
- イベント一覧の下に「開催場所について詳しくはこちら」の大きめなピル型リンクボタンを置き、開催場所一覧ページへ遷移させる。開催場所一覧ページでは、開催場所カードを縦並びで表示し、カード上部に大きな写真枠を確保する。実画像確定までは空のプレースホルダー面とし、下部に場所名、住所、状態チップ、カプセル型の「Googleマップで確認する」ボタンを整理して見せる。
- Firebase 未設定時はカレンダーのみ描画し、「イベント情報は管理画面からの登録後に表示されます。」を `status` 行に表示する安全側の動作とする。
- モバイル（680px 以下）ではカレンダーセル高を縮め、イベント一覧は日付と本文を1カラムに重ねる。
- 旧「静的スケジュールリスト（準備中／企画中の2カード）」および「イベント一覧へ」ボタンは廃止済み（2026-05-23 反映）。

## 今後決めること

- 日本語本文に使うWebフォントを読み込むか、システムフォントを優先するか。
- Image Gen 2 で作る画像の具体的なプロンプト。
- 実際のイベント写真を使用するかどうか。
- メンバー写真を掲載するか、イラスト調にするか。
