# TODO リスト

> 作成日: 2026-05-20

---

## ルール

- このファイルは **サイト仕様・運用方針を変更したが、まだ実装が完了していない項目** を管理する。
- 仕様変更のたびに対応する項目を追加し、実装完了後は「完了済み」セクション（[COMPLETED.md](COMPLETED.md)）へ移動する。
- 各項目には以下を記載する:
  - **優先度**: 最高 / 高 / 中 / 低（長期）のいずれか
  - **対象**: HTML / CSS / assets / Firebase / 管理画面 / 運用 / など
  - **内容**: 変更した仕様の概要と必要な実装内容
  - **状態**: `[ ] 未着手` / `[ ] 計画済み（日付）` / `[ ] 進行中` / `[ ] 内包済み（〇〇）` / `[x] 完了（日付）` / `[x] 廃止（日付）`
    - **`[ ] 内包済み（〇〇）`** … 他TODO（〇〇）の実装で実質的に内包済みだが、整理（廃止移動）が完了していない状態。`autonomous-todo-batch` の整理Phaseで COMPLETED.md へ移動される対象。
  - **計画書**: 実装計画書（`plans/in_progress/*.md` または `plans/completed/*.md`）が作成されている場合はファイル名を記載する
  - **由来**: 備考欄に `由来: 会議` または `由来: 独自` を必ず含める。
    - **由来: 会議** … 議事録・打ち合わせ経由で起こされた項目（先方との読み合わせ対象）
    - **由来: 独自** … 自分が独自に発見・修正した項目（先方への共有は「細かい修正」として一括化）
    - 完了して QAチェックリストに昇格する際、この情報がチェックリストの「区分」列（会議決定 / 独自対応）に引き継がれる
  - **自律実行マーカー**: 備考欄に `自律実行: 可` / `自律実行: 可（〇〇のデフォルトで進める）` / `自律実行: 整理のみ` / `自律実行: 不可（理由）` のいずれかを必ず含める。
    - **`自律実行: 可`** … `autonomous-todo-batch` でそのまま実装可
    - **`自律実行: 可（〇〇のデフォルトで進める）`** … 仕様ドキュメントに複数選択肢があるが、未決定時のデフォルト値を明記して AI 判断で進められる（例: `自律実行: 可（ログイン方式=メール+パスワードのデフォルトで進める）`）
    - **`自律実行: 整理のみ`** … 他TODOに内包済みで、COMPLETED.md への廃止移動のみ自律実行可
    - **`自律実行: 不可（理由）`** … 真に人間判断が必要（例: `自律実行: 不可（外部素材入手・会議経由必須）`）。バッチ実行時の除外対象
  - **ブロッカー / 前提**: 依存関係は以下の2種に分けて備考欄に記載する。
    - **`ブロッカー: 〇〇`** … 解消されないと AI が進められない（素材入手・会議決定・実SMTP契約 等）
    - **`前提: 〇〇`** … 他TODO完了が望ましいが、AI 判断（プレースホルダー方式 / 既存パターン延長）で進められる
- 実装計画書のみ作成された場合は状態を `[ ] 計画済み（日付）` とし、計画書ファイル名を記載する。
- 廃止になった項目は削除せず `[x] 廃止` として残し、廃止理由を記載する。
- 項目IDはアルファベット+連番（例: A-1, B-1）で管理し、セクションをまたいで一意にする。
- **IDの再利用は禁止**。新規IDを採番する際は、TODO.md と COMPLETED.md の両方を確認し、既存の最大番号の次を使用すること（例：B-1〜B-4が完了済みなら、次のバグ修正はB-5から）。

---

## A. コンテンツ追加（記事・イベントレポート等）

| ID | 優先度 | 対象 | 内容 | 状態 | 備考 |
|----|--------|------|------|------|------|
| A-1 | 中 | HTML / assets | トップページに「モルキニストの第一印象」セクション（集合写真＋活動内容紹介）を配置 | `[x] 廃止（2026-05-21）` | 2026-05-20 追加 / 2026-05-21 C-1に統合（廃止）／ 由来: 会議 |
| A-2 | 中 | HTML | トップページに「次回イベントカレンダー」セクション（仮：静的版）を配置 | `[x] 廃止（2026-05-21）` | 2026-05-20 追加 / 2026-05-21 C-1に統合（廃止）／ 由来: 会議 |
| A-4 | 中 | assets / HTML | ココシバの実ロゴ画像を入手して `assets/images/partners/` に配置し、`index.html` の `#partners` セクションのココシバカードのプレースホルダーを差し替える。 | `[ ] 未着手` | 2026-05-21 追加（A-3 整理時に分離） / 由来: 会議 / **自律実行: 不可**（外部素材入手・ココシバ運営との合意必須） / **ブロッカー**: ココシバ実ロゴ画像の入手と掲載許諾 |

---

## B. バグ修正（修正必須）

| ID | 優先度 | 対象 | 内容 | 状態 | 備考 |
|----|--------|------|------|------|------|

---

## C. UI/UX改善

| ID | 優先度 | 対象 | 内容 | 状態 | 備考 |
|----|--------|------|------|------|------|
| C-1 | 高 | HTML / CSS | トップページを参考サイトの構成方針に寄せ、`Molkkynistについて（モルキニストの第一印象：集合写真＋活動内容紹介を含む）`、`次回イベント / 開催スケジュール（静的版カレンダーを含む）`、`参加の流れ`、`活動レポート`、`メンバー`、`よくある質問`、`Instagram DM`、`協力団体・スポンサー（ロゴ並べ）` の順で、参加判断に必要な要約が1ページ内で追える構成に再整理する。既存の複数ページ構成は残し、トップページから各詳細ページへ導線を置く。 | `[x] 完了（2026-05-21）` | 由来: 会議。参考: https://molkky.co.jp/#about の構成比較結果。2026-05-21 更新: 末尾に「協力団体・スポンサー」を追加（R-5決定）、旧 A-1 / A-2 をサブ要件として統合。 |

---

## D. 新機能（フォーム・管理画面・Firebase連携等）

| ID | 優先度 | 対象 | 内容 | 状態 | 備考 |
|----|--------|------|------|------|------|
| D-1 | 高 | HTML / Firebase | 参加・お問い合わせフォームをサイト内に実装（Firebase連携の独自フォーム）。必須: 名前・メール／任意: 電話番号／用件区分（参加希望・イベント問い合わせ・メディア取材・その他）／一言／参加希望時は Firestore `events` から開催日程を取得しトグル選択（複数選択可）。Instagram DM 動線と併設する。 | `[x] 完了（2026-05-21）` | 2026-05-20 追加 / 2026-05-21 仕様確定（R-1決定）／ 2026-05-21 実装完了（HTML/CSS/JS、`js/firebase-config.js` の値投入後に有効化）／ 由来: 会議 / 依存: F-1（プライバシーポリシー先行・完了）、D-2（管理画面側のイベント日程登録機能・残）、D-3（自動返信メール・残） |
| D-3 | 高 | Firebase / 運用 | お問い合わせフォーム送信時に、ユーザーのメールアドレス宛へ受付完了の自動返信メールを送る仕組みを実装（Cloud Functions + 外部SMTP）。DM経由の申込時の受付メール送信フローは未定（管理画面から手動送信するか、別途検討が必要）。 | `[x] 完了（2026-05-21）` | 2026-05-21 追加（C-1 参加の流れ ステップ②要件由来） / 2026-05-21 実装完了（Cloud Functions + Trigger Email Extension 方式） / 2026-05-22 GrouMap と同じ Cloud Functions + nodemailer + Gmail SMTP 直送方式へ変更 / 由来: 会議 / 依存: D-1 / DM経由フローは要検討（R-8） |
| D-5 | 高 | Firebase / 管理画面 / 運用 | Firebase 本番運用の残作業を完了する。Authentication で石井さん用ユーザーを作成し、UIDを `js/firebase-config.js` / `firestore.rules` / Functions 環境変数 `ADMIN_UID` に反映する。Gmail SMTP 用 Functions Secret を設定し、テスト送信を確認する。 | `[ ] 進行中` | 2026-05-21 追加 / 2026-05-21 Firestore は `asia-northeast1`（東京）で再作成済み、Cloud Functions は `asia-northeast1` にデプロイ済み / 2026-05-22 自動返信メール実装を Gmail SMTP 直送方式へ変更 / 由来: 独自 / **自律実行: 不可**（Authユーザー作成、Googleアカウント・アプリパスワード発行が必要） / **ブロッカー**: 石井さん用ログイン情報、送信用Googleアカウント、SMTPアプリパスワード |
| D-6 | 高 | Firebase / 運用 / DNS | 自動返信メールを GrouMap と同じ Gmail SMTP 方式で運用する。送信元メールを Google Workspace 独自ドメインメールにするか無料 Gmail にするかを R-9 で決定後、対象アカウントの2段階認証とアプリパスワードを準備し、Functions Secret に `SMTP_USER`、`SMTP_PASS`、`SMTP_FROM` を設定する。設定後、フォーム送信から自動返信メールの受信までテストする。 | `[ ] 進行中` | 2026-05-21 追加 / 2026-05-22 Gmail SMTP 方式に決定し、Functions 側の nodemailer 直送実装は完了。`SMTP_HOST=smtp.gmail.com`、`SMTP_PORT=587`、`SMTP_SECURE=false` は設定済み / 由来: 独自（ユーザー申告: SMTP未対応、GrouMap方式踏襲） / **自律実行: 不可**（送信元メール方式の決定、Googleアカウント作成・2段階認証・アプリパスワード発行が必要） / **ブロッカー**: R-9の決定、送信用Googleアカウント、SMTPアプリパスワード、Secret設定後の本番テスト / 関連: D-5、R-9 |

---

## E. SEO / パフォーマンス

| ID | 優先度 | 対象 | 内容 | 状態 | 備考 |
|----|--------|------|------|------|------|

---

## F. その他

| ID | 優先度 | 対象 | 内容 | 状態 | 備考 |
|----|--------|------|------|------|------|
| F-1 | 高 | 運用 / HTML | プライバシーポリシーの整備（お問い合わせフォーム導入に伴う、個人情報の取り扱いに関する明記）。D-1 公開の前提条件。 | `[x] 完了（2026-05-21）` | 2026-05-20 追加 / 2026-05-21 D-1依存に伴い優先度を中→高に変更／ 2026-05-21 `privacy.html` を新規作成し全ページfooterから導線設置 / 由来: 会議 |

---

## 完了済み

> 完了済みタスクの詳細は [COMPLETED.md](COMPLETED.md) を参照。

---
