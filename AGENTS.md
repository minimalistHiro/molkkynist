# AGENTS.md

このプロジェクトでのアシスタント応答・実装ルール:

- 返答は必ず日本語で行うこと。
- このプロジェクトは静的Webサイトとして制作する。
- 基本構成は HTML / CSS を優先し、JavaScript は必要になった場合のみ追加すること。
- 既存のファイル構成と命名規則を尊重し、不要なフレームワークやビルドツールは追加しないこと。
- 画像やフォントなどのアセットは `assets/` に配置すること。
- CSS は `css/styles.css` を基準に管理すること。
- 画面追加やデザイン変更を行う場合は、モバイル表示とデスクトップ表示の両方で破綻しないように確認すること。
- テキストやコメントは、原則として日本語で記述すること。
- 管理画面でFirestoreなどのデータベース項目を一覧表示する場合は、各項目に編集ボタンと削除ボタンを並べて設置すること。削除は編集フォーム内ではなく、作成済み項目の一覧側から実行できるようにし、必ず確認ダイアログを挟むこと。

## HTML/CSS 方針

- セマンティックHTMLを優先する。
- 見出し階層を崩さない。
- レイアウトはレスポンシブ対応を前提にする。
- 色・余白・フォントサイズはCSSカスタムプロパティで管理する。
- 装飾よりも可読性と更新しやすさを優先する。

## 確認方法

- 静的ファイルのみの構成なので、基本的には `index.html` をブラウザで開いて確認する。
- ローカルサーバーが必要な場合は、プロジェクト直下で `python3 -m http.server 8000` などを使う。

## プロジェクト資料の参照ルール

以下の話題が出た場合は、該当するMarkdownを確認してから回答・実装すること。

- プロジェクト全体方針、目的、ターゲットに関する話題:
  - `PROJECT_OVERVIEW.md`

- サイト構成、ページ追加、画面設計、ナビゲーションに関する話題:
  - `SITE_STRUCTURE.md`

- デザイン、配色、画像素材、Image Gen 2、レイアウトに関する話題:
  - `DESIGN_GUIDELINES.md`

- 文章、コピー、イベント告知文、活動レポート文、Instagram誘導文に関する話題:
  - `CONTENT_GUIDELINES.md`

- Firebase、Firestore、Authentication、Storage、Hosting、権限管理に関する話題:
  - `FIREBASE_ARCHITECTURE.md`

- 管理画面、石井さん専用ログイン、イベント編集、レポート編集、メンバー編集に関する話題:
  - `ADMIN_REQUIREMENTS.md`

- 開発順序、実装フェーズ、今後の進め方に関する話題:
  - `ROADMAP.md`

該当する話題で実装や設計判断を行う場合は、先に関連Markdownを確認し、既存方針と矛盾しないようにすること。
